# MobTrace Mobile Validation Fixture

This fixture is a standalone synthetic mobile project used to validate
MobTrace product behavior without a real emulator.

The `bin/maestro` script simulates Maestro outcomes from the flow file name.
Tests copy this fixture into a temporary Git repository before each scenario.
